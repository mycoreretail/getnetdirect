GETNETDIRECT v11 — SHARED SYSTEM

Frontend repository: mycoreretail/getnetdirect
API: https://getnetdirect-api-production.up.railway.app
Existing API root directory: api
Start command: npm start

Keep DATABASE_URL and JWT_SECRET in Railway environment variables, never GitHub.
Existing ADMIN_USERNAME / ADMIN_PASSWORD bootstrap variables do not overwrite an
existing user on restart. Account credentials already in the database remain.
Every team/partner account and code must exist in the shared database; codes made
only in old browser-local prototypes are not automatically imported.

The API automatically applies additive v11 schema changes at startup. Read the
backup, upload and verification steps in V11-UPDATE-INSTRUCTIONS.txt.
